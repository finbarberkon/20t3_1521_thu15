#include <stdio.h>
#include <string.h>

#define MAX_NAME_LENGTH 256
#define MAX_BREED_LENGTH 64

struct pet {
    //name
    //breed
    //age
    //weight
};

int main(void) {
    // Declare a (local) variable of type struct pet

    // Set each of its fields

    return 0;
}
